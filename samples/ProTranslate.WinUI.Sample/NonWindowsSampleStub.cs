namespace ProTranslate.WinUI.Sample;

public static class NonWindowsSampleStub
{
    public const string Message = "The WinUI sample app builds on Windows. Non-Windows builds compile this stub so the full solution remains portable.";
}
